export function DashboardPage() {
  return (
    <div>
      <h1 style={{ fontSize: '1.25rem', fontWeight: 700, color: 'var(--app-text)' }}>
        Dashboard
      </h1>
      <p style={{ color: 'var(--app-text-muted)', marginTop: 8 }}>
        Porteføljeoversikt — kommer snart
      </p>
    </div>
  );
}
